---
name: print-cmyk
description: 惜食廚房大圖送印產線：外牆防水布 12 面、歷史發展牆（直角梯形異形）、一樓乾貨牆志工名單、便當捐款卡。含 CMYK PDF 輸出、出血/裁切/安全線、字體加大、異形幾何與所有已知地雷。要做防水布、大圖、送印檔時載入。
---

## 惜食廚房外牆防水布（最新：招募志工面，2026-07-07）
- 主牆定稿 `外牆防水布_主牆25m5_招募志工版.png`（25500×1300，第7面已改成志工招募：暖光志工廚房底圖＋5項服務金圓圖示＋3步驟＋QR「了解更多/立即報名」＋扶輪3501捐贈框）。腳本 scratchpad/vol_panel2.py。規格與地雷見 memory `savefood-kitchen-signage`。
- **立即報名 QR 更新（2026-07-07）**：右 QR 改指志工報名表單 `https://forms.gle/Njoas8PnUuEuZCqk7`。腳本已遺失，改「就地替換」：偵測白卡內黑模組定位右QR(x24762~25098/y652~988)→`qrcode`(border=0,EC=Q)產330px置中蓋回→清白舊區防殘影。左「了解更多」官網QR未動。原檔備份 `外牆防水布/備份_20260703/..._改QR前_20260707.png`。**這台無 pyzbar/opencv 可解碼**，QR正確性靠「直接用URL產生」保證，送印前請手機實掃
- **3501致贈框位置調整（2026-07-12）**：從主牆志工面右上角**移除**(用框下方乾淨照片逐欄垂直內插補背景，端點接合無矩形補丁)，改放**次牆14m45第一面**右上空白框(填深綠#084D22粗體「國際扶輪3501地區捐贈」置中)。就地替換、原檔備份於 `備份_20260703/`
- **新增單面布「惜食台灣行動協會」（2026-07-12）**：`防水布_惜食台灣行動協會_3.6x1.25m.png`(3600×1250=1000px/m)，白底+品牌綠外框+惜食台灣logo(HQ透明版`二樓面外大logo/惜食台灣_logo_HQ.png`，3332×3332，alpha bbox 410,574,2925,2765)左+協會名綠字+橘副標「疼惜食物·疼惜台灣」。⚠️**外框色實測＝`#079A3F`**（本文件原記 #079E3F 為誤，2026-07-21 實測更正；用錯會與舊布有肉眼可辨色差）
- **✅單面布定案「惜食廚房版B」（2026-08-03）**：母檔 `防水布_惜食台灣行動協會_惜食廚房版B_3.6x1.25m.png`，腳本 `design/tarp_solo_kitchen.py`（一次產 A/B 兩版比稿，A 版已刪）。版面＝logo／協會名小標／「惜食廚房」主標 560px／金色標語「等一個便當 等一份疼惜」。送印檔已換：`python3 design/tarp_face_pdf.py <母檔> 0 0 <輸出pdf>`（**x0=x1=0＝整張不裁**）→ `輸出用_分面PDF_調亮100dpi/單面布_…_3.600x1.25m_CMYK.pdf`(14173×4921px＝100dpi)，舊團體照版移入同層 `備份_舊版_團體照版_20260803/`
  - ⚠️**驗色不可用 `(1-C)(1-K)` 粗略 CMYK→RGB 近似式**：實測會把主綠算成 (51,228,12)、與模擬圖 (7,152,62) 差 76，看似大爆色其實是公式錯。要用 `ImageCms.profileToProfile` 走同一支 ICC 反轉回 sRGB 才量得到真值 (32,154,75)。**殘差 ΔR≈25 是 CMYK 色域極限**（品牌鮮綠 #079A3F 印刷印不到），非流程有誤
  - **母檔偏暗屬正常**（預先反向補償下游 enhance 的 1.65/1.83），審稿一律看 `預覽_惜食廚房版B_印刷模擬.png`
- **單面布團體照版（2026-07-21，已於 2026-08-03 廢除）**：腳本 `design/tarp_solo_photo.py` 已刪，以下為通則留存。左 logo／中兩行主標／右圓角照片卡
  - **主標必須拆兩行**「惜食台灣」／「行動協會」字級維持300不縮：照片卡佔右側1136px後左欄僅剩1250px，單行8字寬2375px得縮到139px、牆面遠觀即失效
  - 座標：logo(287,209,706×616)／主標L1(1090,190)行距370／副標138px居中cx=1682／照片卡(2374,100,1136×1050)圓角44、綠描邊12、內圓角32。**照片卡2374是維持標題300的極限，不可再左移**
  - **照片必須反向補償 `tarp_face_pdf.py` 的 enhance()**（無條件套 Brightness1.65＋Color1.83＋UnsharpMask(2,120,3)，為平面插畫調的，照片走這條會爆白）。⚠️**純線性壓暗不夠**（0.78/0.68 實測整體過曝9.17%＝牆面死白）；正解＝**亮部壓縮曲線** `tone_compress()`：膝點130以下線性×0.82保住臉、以上ease-out收斂到145拉住高光，Color 0.64 → 過曝3.60%且臉部更亮。**補償烘進PNG母檔、不改 tarp_face_pdf.py**（該腳本為12面共用統一階段，加分區例外會破壞其他面）；故母檔看起來偏暗屬正常
  - 照片素材＝FB下載版1662×1534已壓縮，卡片比例做成貼合原生1.0834故**不需裁切**，招牌與所有人物全留。**寬度上限1200px**否則低於35dpi；現用1112px＝38dpi，實測人臉1:1印刷尺度五官清楚可辨
  - 銳利化只用45%（非120%）：下游還會再套一次UnsharpMask，兩段重銳利會讓每顆頭出現白邊光暈。**勿用MedianFilter(3)**（1662px尺度會吃掉眼睛與眼鏡框），LANCZOS縮小本身即最佳去JPEG塊
  - **不加陰影**：灰階柔和漸層在純白底經1.65提亮再走100dpi四色分色會印成帶狀或髒污；12px綠描邊是本專案既有語彙
  - 待辦：照片含春聯屬**春節時效元素**長期掛設會顯舊；25+可辨識人臉印3.6m公開展示，**肖像權需協會確認**
- **CJK 文字定位地雷**：`ImageDraw.textbbox` 對中文回傳的是 layout box（4字×300=1200）不是墨色框（1184）。需 render 到暫存 `L` 影像後取 `.getbbox()`（見 `tarp_solo_photo.ink_bbox`）。多行標題的 pen-x **只由第一行算、其餘沿用**＝視覺齊頭；各字字面高度不同故墨色框容許數px差，驗證行距勿卡死±2px
- **⚠️防水布輸出一律 CMYK（2026-07-12 使用者指定）**：PNG不支援CMYK。轉換走 `ImageCms`+系統`/System/Library/ColorSync/Profiles/Generic CMYK Profile.icc`(PERCEPTUAL)，**勿**用PIL粗略`.convert("CMYK")`。詳見 memory `feedback_banner_cmyk`
- **⚠️交印刷廠改用 CMYK PDF、勿交 CMYK TIFF（2026-07-15）**：三冠彩印的RIP把PIL產的CMYK TIFF油墨值反相解讀（整張負片化：白變黑、綠變暗紫）。改交純Python自組PDF（DeviceCMYK+FlateDecode、頁面1:10縮尺、內嵌影像升採樣全尺寸72dpi），大檔用分段串流寫入（8GB機器直接產72dpi大TIFF會寫壞檔）。**最終交付＝`輸出用_分面PDF_調亮100dpi/` 12份分面1:1 PDF**（2026-07-16：使用者反映偏暗＋字小，全面調亮版取代舊版——主標字放大1.2倍(遮罩取字+膨脹修補背景防殘影，多行區域不可重疊)、亮度+10%/飽和+22%/銳利化、100dpi全尺寸），腳本 scratchpad/enhance_faces.py＋make_face_pdfs_v2.py；舊72dpi版 `輸出用_分面PDF/`、整條1:10版留存備用
- **全面加大字體（2026-07-17，印刷反映字不清楚）**：11 面母檔 PNG 就地放大文字（裁字區→LANCZOS放大→羽化貼回；招募志工面最徹底：中欄5項×1.35/右欄3步驟×1.3/左欄小字×1.25~1.35；其餘面英文副標×1.45~1.5、中文小標×1.3~1.35、主標×1.15~1.2 補回母檔）→ 12 份 `輸出用_分面PDF_調亮100dpi/` 全部重產並 sips 抽驗無反相。腳本已進 repo：`design/tarp_text_enlarge.py`(apply_ops通用)＋`design/tarp_text_enlarge_all_faces.py`(各面ops座標)＋`design/tarp_face_pdf.py`(單面調亮100dpi CMYK PDF，參數=母檔/x0/x1/輸出)。改前母檔備份於 `備份_20260703/*_加大字體前_20260717.png`。地雷：羽化寬度必須≤裁切留邊、勿用dy位移（原字露出殘影）；`分面_印刷ai檔/` 72dpi .ai 版未重產仍是舊字
- **改版：次牆縮短＋志工面移位（2026-07-21）**：次牆右段 4.03m 牆面無法施作須留空，可用長度剩 10.42m。**志工招募面(4.802m)從主牆末面移到次牆第一面**、惜食5招(4.000m)遞補主牆第7面。最終＝主牆7面24.698m(餘0.802m當掛設彈性餘量不補布)／次牆2面8.400m(右側6.05m裸牆)。退場兩面＝一餐得來不易(2.850)、疼惜食物疼惜台灣(4.002)，移至 `輸出用_分面PDF_調亮100dpi/未使用_20260721/`。**PDF 零重產**（各面為獨立板塊、上下綠框自帶，重排順序即可），9份已依掛布順序改名 `主牆_01~07`／`次牆_01~02`，附 `掛布順序說明_20260721.md`
- ⚠️**每面右上角是不同扶輪地區的捐贈掛名**（主牆3523/3522/3521/3482/3481/3490、次牆「你的小動作」3501），**刪面＝刪掉該地區露出，動面前必查**；空白框面（惜食5招/一餐得來不易/疼惜食物）才可安全退場
- ⚠️**面寬離散、湊不出剛好**：主牆恆缺 0.5~0.8m。**背景延伸不可行**——惜食5招的圓圈1/5距邊僅167px，鏡像會複製出反字；一餐得來不易的扶輪框與農夫都在右側800px內，裁窄即毀。原始設計稿與生成腳本均已不存，無法重新排版產出，只能整面取捨
- **鮮豔度/亮度再各 +50%（2026-07-17，使用者要求）**：`design/tarp_face_pdf.py` 的 `enhance()` 改用常數 `BRIGHTNESS=1.65`（前1.10×1.5）＋`COLOR=1.83`（前1.22×1.5），12 面全部重產(3.8分)並 sips 抽驗無反相。過曝(≥250三通道)僅 9.14%→9.82% 不爆（白底本就白、變亮的是彩色圖文區）。舊 1.10 版備份 `外牆防水布/備份_分面PDF_舊亮度1.10_20260717/`。⚠️飽和1.83偏強會把近中性字推色（志工面「傳遞溫暖與愛」偏橘、QR標籤偏綠），嫌太跳改折衷 亮1.35/飽1.50

## 惜食廚房歷史發展牆（異形／2026-08-06 改梯形）
腳本 `design/trap_wall_history.py`，產物在 `惜食廚房輸出/三角牆_歷史發展/`（資料夾名未改）。
取代原規劃的「三角牆活動相簿 295/180/350」與「歷屆理事長牆 295×90」，兩案皆已取消。

| 項目 | 內容 |
|------|------|
| 尺寸 | **直角梯形**：頂 295／左 250／右 85／**斜底 338.0**，直角在左上與右上，斜底由左下升到右下。面積 4.94 m² |
| 角度 | 斜底對水平 29.2°；左下 60.8°、右下 119.2° |
| 版面 | 標題「認識惜食廚房」＋副標／右上角數據「22萬+ 累積送出愛心餐盒」（2022 數字待協會更新）／時間軸沿斜底 2015→2023 六色分段／照片 6 格 3-2-1 階梯，各約 66×36cm＝0.24 m² |
| 用法 | `--dpi 100 --div 1`（1:1 母檔）／`--div 4`（草稿）／`--cells`（畫照片格）／`--nofill`（只留空格不填模擬照片）／`--bg plain\|warm\|band\|stripe` |
| 現況 | **底圖已定案**（`--bg band`：品牌綠沿斜底 78cm 往內漸淡）；100dpi＝11810×10039px、約 20 秒、3.0MB。**卡在協會提供 6 張照片原始檔** |

- ⚠️**量測值不自洽時，以直邊為準反推斜邊**：現場給 250/295/85/325，但直角梯形三條直邊一定，第四邊就被幾何鎖死＝√(295²+165²)=**338.0**，與量到的 325 差 13cm（遠超 2.5cm 出血）。直邊貼牆好量、斜邊拉尺易失準。四邊全採信的唯一解是右邊向內傾 10.8°，不合理。**別擅自把差額吃進出血，也別擅自直角化**
- ⚠️**四邊各自內縮，不能一律內縮**：時間軸帶只佔斜底側，其餘三邊只需 6cm 安全距。函式＝`quad_pts(left, top, right, slant, ...)`，斜底沿法線平移後與左右兩股求交。**斜底縮到切過上緣時要退化成三角形回傳**，背景漸層/條紋會用到大 inset
- ⚠️**法線務必真的垂直**：`(-uy, ux)` 才是法線，寫成 `(-uy, -ux)` 不垂直（點積 0.85），年份與標題會壓在時間軸線上。改形狀時這行最容易抄錯
- **照片切格**用固定帶高、逐帶以**底緣**（最窄處）算可用寬，格子才不會右下角戳出斜底。`CELL_H_CM=36`＋`MIN_CELL_W_CM=58` 才切得出 3-2-1 共 6 格；38 只會切出 5 格
- **驗證墨色沒越過安全線時，要先排除出血區的裁切標記**（它們本來就畫在成品框外），否則會誤報數千像素越界
- 照片需求：**6 張長邊 ≥2600px、比例約 1.85:1**，cover 模式自動裁切居中。現行 `模擬照片/01~06.jpg` 是從協會簡報裁的（整頁只有 960×540），**只能看版面不可送印**
- 文案與六色配色取自協會 2025 簡報第 006 頁橫條，與協會既有視覺一致。沿斜底的旋轉文字用 `draw_rot_text()`，標籤須短於節點間距（現 54cm）故壓在 6 字內

**異形輸出通用地雷**（本專案第一面非矩形輸出留下，之後做異形都適用）：
- **異形出血**：各邊沿自身法線平移，不可把頂點往內移（那樣斜邊的縮距會不等於設定值）。出血 −25／裁切線 0／安全線 +60（mm），綠帶跨過裁切線，裁切公差內只會讓邊變粗細不露白。裁切標記畫在 CUT 外 30~55mm，**銳角處不畫角落延伸線**（會壓到鄰邊）
- ⚠️**`ImageFilter.MinFilter` 會先複製邊界像素再侵蝕**，貼齊影像邊界的直線邊完全侵蝕不到。做描邊必須先 `ImageOps.expand(border=w, fill=0)` 再 crop 回來，否則只有圓角有綠邊、直線邊全裸
- ⚠️**佔位灰必須 `(136,136,136)`**：下游 ×1.65 後為 (224,224,224) 是舒服的淺灰；(150,150,150)→(247,247,247) 幾乎看不見、(228,228,228)→純白格子直接消失
- ⚠️**STHeiti 沒有 `▸`**（同既有 `・`/`／` 豆腐地雷）
- 母檔 1px=1mm＝25.4dpi 是硬天花板。若 1:1 預覽不夠實，本地另寫 2px/mm 版本，**不要改 `tarp_face_pdf.py`**（12 面共用階段）

## 惜食廚房一樓乾貨牆 · 志工名單（可拆換）

把志工／廚師照片填進展示牆。**2026-08-03 尺寸更正為 300×120cm**（原 342×146，v20 全套已刪）。

| 項目 | 內容 |
|------|------|
| 產線 | `design/dry_goods_wall_base_300x120.py`（底圖）→ `overlay_dry_goods_wall_v21.py`（疊照片）→ `dry_goods_wall_cmyk_300x120.py`（送印）|
| 母檔 | 3150×1260px（1px=0.952mm）；送印 `WALL_W=8504`＝72dpi |
| 版面 | 標題帶／廚師排 5 格／志工 **10 欄×4 列＝40 格**（現 36 位）|
| 照片 | `…/志工照片/{志工\|廚師\|廚助}-<姓名>.{jpg,png}`，**前綴決定位置、檔名即姓名**，同名優先 png |
| 送印 | 含 **1cm 出血**，`MediaBox` 302×122cm、`TrimBox` 為成品框 |

- ⚠️**比例 2.3423→2.5 不能等比放大**。底圖重組法＝頂部帶(含標題)與底部裝飾帶**原樣保留不變形**，中段側欄食材圖示**逐個平移**吸收掉少掉的 84px（不縮放才不會把插畫壓扁），格子區整片洗成米色
- ⚠️**格子的水平範圍動不了**：左右側欄圖示（左緣到 x193）與**右下官網 QR 面板**（x2870 起）鎖死可用寬，10 欄硬撐滿版會壓上去。故 `VW_FRAC=243.58/3148` 沿用舊格寬、只讓 vh 吃掉多出的高度 → 格子不再是 A5，照片框比例 2.03→1.76（更適合人像）
- ⚠️**格子區洗白後，白卡／橘條／灰框剪影就沒了**——342 版那層是畫在底圖上的，v21 必須自己重畫（`card()`）。漏了會變成「照片浮在米色底、空格消失」
- ⚠️**`dry_goods_wall_v9_clean_base.png` 不可刪**：原始 AI 底圖 `v5_final.png` 早已不存在，它是 300×120 底圖的**唯一來源**
- ⚠️**外部依賴換 session 會消失**：`u2net.onnx`(168M，github rembg v0.0.0 release，落 `~/.cache/`)＋`Unihan_IRGSources.txt`(unicode.org UCD Unihan.zip)，走 env `U2NET_ONNX`／`UNIHAN_TXT`
- ⚠️**志工照片會被 iCloud 移出本機**（Desktop 在 iCloud Drive，**磁碟滿時系統自動 evict**成 `.檔名.icloud` stub）。跑之前先數 `ls 志工照片 | grep -c '^志工-'`，少於預期就是被 evict。**空間一釋放通常就會自己下載回來**，`brctl download` 在磁碟滿時無效

## 惜食便當捐款卡轉 .ai（2026-07-07）
- 來源 `惜食廚房輸出/惜食便當捐款卡/惜食便當捐款卡_印刷40cm.png`（4724×4724=40cm@300dpi，成品點陣圖含Logo/愛心/漸層，無法真向量化）。交印刷廠用 reportlab 產 **PDF相容 .ai**（`drawImage` 內嵌300dpi，`mask='auto'` 保留透明SMask）。
- **最終定案＝只底部立體邊版**(2026-07-09 改)：只取奶白圓角卡本體(x455~4268/y503~4055,圓角120)、**外圍米色底盤去背透明**，用輪廓掃掠(逐步offset paste同色遮罩,近NEAR#DBC99C→遠FAR#A88E58)做出牛皮紙盒身**只往下的底部立體邊**(D=150,只位移y、無右側立體)。畫布3813×3702px=32.3×31.3cm。輸出 `惜食便當捐款卡.ai`+`_立體_透明.png`+`_立體_預覽.png`。腳本 scratchpad/bento_bottom_only.py。過程反覆：整張box→只本體→回原版去背→加回立體(側+底)→厚度減半→**最後拿掉右側只留底部**。舊「側+底」版備份於同資料夾 `備份_側邊底邊版_20260709/`
