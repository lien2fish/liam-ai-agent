/**
 * 助理設定檔 —— 唯一需要改的檔案。worker.js 不含任何商業資訊。
 *
 * ┌─ 必填 ─────────────────────────────────────────────┐
 * │ owner / business / tone / vocabulary / github        │
 * │ 填完這些就有一個能對話、能語音、能記筆記的助理了。   │
 * └─────────────────────────────────────────────────────┘
 *
 * 底下的「選用」區塊是接自己資料庫用的，一開始全部留空即可——
 * 留空的功能會自動關閉，指令和工具清單都不會出現。
 */

export default {
  // ══════════════════════════════════════════════════════
  //  必填
  // ══════════════════════════════════════════════════════

  owner: {
    name: "老闆",
    // 助理是誰、服務誰。會直接放進 system prompt。
    intro: "你是（公司名）負責人的手機助理，透過 LINE 對話。",
  },

  // 事業背景。寫得越具體，它越懂你在講什麼。
  business: `（品牌、產品線、你在意什麼）`,

  // 回覆風格。這是手機聊天視窗，不是報告。
  tone: `- 一律繁體中文，**簡短**，通常 3 行以內講完。
- 不要用 Markdown 表格（LINE 不會渲染）。多筆資料用「・」條列。
- 金額寫成 12,500 這種帶千分位的形式。
- 查不到資料就直說查不到，不要猜。`,

  // 使用情境。幫它判斷你丟一句話過來是要它做什麼。
  context: `他常在外面用手機打字，句子會很短、可能有錯字，用常識判斷他的意思。
他丟一句想法過來通常是要你記下來，不是要你分析。`,

  // 你這行的專有名詞。Whisper 拿去提高語音辨識率，差很多。
  // 地名、品名、產業術語、常講到的人名都放進來。
  vocabulary: [],

  // 待辦與知識庫寫到哪。**務必用私人 repo**，那裡會累積你的商業知識。
  github: {
    workspaceRepo: "你的帳號/你的私人repo",
    todoPath: "TODO.md",
    knowledgeDir: "knowledge",
    agentRepo: null, // 選用，見下方
  },

  // 口述的內容會依這個分類歸檔。改成你自己的分類。
  knowledgeCategories: {
    misc: "其他",
  },

  // ══════════════════════════════════════════════════════
  //  選用 —— 接自己的資料庫
  //
  //  一開始全部留空。等助理用順了、資料也整理進 Notion 了再回頭開。
  //  每個區塊的欄位名稱都要**實際打開你的資料庫確認**，
  //  不要假設不同資料庫的欄位長得一樣——猜錯不會報錯，只會查出一片空白。
  // ══════════════════════════════════════════════════════

  // 查客戶 / 查銷售。db 填的是 wrangler.toml [vars] 裡的變數名稱。
  crm: null,
  // crm: {
  //   customer: { db: "CUSTOMER_DB", titleField: "客戶姓名",
  //               fields: ["客戶姓名", "聯絡電話", "累計消費", "最後購買日"] },
  //   sales:    { db: "SALES_DB", matchField: "客戶名稱", sortField: "出貨日期",
  //               fields: ["出貨日期", "品項", "數量", "金額"] },
  // },

  // 查庫存。每個品牌一組欄位對照。
  inventory: [],
  // inventory: [
  //   { brand: "品牌A", db: "INV_A_DB", name: "品名", qty: "庫存數量", unit: null, price: "進價" },
  // ],

  // 觸發／查詢 GitHub Actions。需要上面 github.agentRepo 一起設。
  workflows: {},
  // workflows: { 日報: "daily_report.yml" },

  // 其中會對外發布、收不回來的任務。這些不接受斜線指令，
  // 因為斜線指令不經過 AI、沒有確認步驟，手機誤觸的代價收不回來。
  publishes: [],
};
